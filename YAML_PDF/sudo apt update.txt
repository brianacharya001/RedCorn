sudo apt update
sudo apt install python3 python3-pip wkhtmltopdf
pip3 install pyyaml jinja2


title: Sample Document
author: John Doe
date: 2024-08-28
sections:
  - title: Introduction
    content: |
      This is an introduction to the sample document. It explains the purpose and contents of the document.
  - title: Main Content
    content: |
      Here is the main content of the document. It contains detailed information about the subject.
  - title: Conclusion
    content: |
      The conclusion summarizes the key points and offers final thoughts.


import yaml
from jinja2 import Template
import pdfkit

# Load YAML data
with open('data.yaml', 'r') as file:
    data = yaml.safe_load(file)

# Define HTML template
html_template = """
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        h1 { font-size: 24px; }
        h2 { font-size: 20px; margin-top: 20px; }
        p { font-size: 16px; }
    </style>
</head>
<body>
    <h1>{{ title }}</h1>
    <p><strong>Author:</strong> {{ author }}</p>
    <p><strong>Date:</strong> {{ date }}</p>
    {% for section in sections %}
        <h2>{{ section.title }}</h2>
        <p>{{ section.content }}</p>
    {% endfor %}
</body>
</html>
"""

# Render HTML from template and data
template = Template(html_template)
html_content = template.render(
    title=data['title'],
    author=data['author'],
    date=data['date'],
    sections=data['sections']
)

# Save HTML to file
with open('document.html', 'w') as file:
    file.write(html_content)

# Convert HTML to PDF
pdfkit.from_file('document.html', 'document.pdf')

print("PDF generated successfully.")



python3 convert_yaml_to_pdf.py
